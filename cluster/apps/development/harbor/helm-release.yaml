---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: harbor
spec:
  interval: 5m
  chart:
    spec:
      # renovate: registryUrl=https://charts.bitnami.com/bitnami
      chart: harbor
      version: 11.0.4
      sourceRef:
        kind: HelmRepository
        name: bitnami-charts
        namespace: flux-system
      interval: 5m
  valuesFrom:
    - kind: Secret
      name: harbor-admin-password
      valuesKey: HARBOR_ADMIN_PASSWORD
      targetPath: harborAdminPassword
  values:
    trivy:
      ignoreUnfixed: true
    global:
      storageClass: rook-fast-rust-block
    externalURL: https://harbor.echozulu.games
    service:
      type: Ingress
      tls:
        existingSecret: harbor-${SECRET_DOMAIN/./-}-tls
        # notaryExistingSecret:
    ingress:
      enabled: true
      annotations:
        traefik.ingress.kubernetes.io/router.entrypoints: "websecure"
      hosts:
        core: harbor.echozulu.games
        notary: notary-harbor.echozulu.games
